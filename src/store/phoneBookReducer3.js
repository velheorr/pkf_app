

const defaultState = {
    list: [],
    finder: '',
    sortedList: null,
    user: [],
};

export const phoneBookReducer3 = (state = defaultState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}

